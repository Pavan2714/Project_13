# Core Flask & Web Server
setuptools==70.0.0  # Ensures setuptools is installed first
python-distutils==1.0.0  # Explicitly includes distutils
Flask==2.3.3
flask-cors==5.0.1
Flask-Login==0.6.2
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
gunicorn==23.0.0
Werkzeug==2.3.7
python-dotenv==1.0.0

# Lightweight utilities (optional)
requests==2.31.0        # For API calls (replace heavy ML with external APIs)
numpy==1.24.3          # Only include if absolutely needed (adds ~15MB)
Pillow==10.0.1         # For basic image processing (if required)